<% layout("/layouts/boilerplate") -%>

<div class="row mt-3">
    <h1 class="col-6 offset-3">Signup in the wanderlust</h1>
    <div class="col-6 offset-3">
      <form action="/signup" method="post">
        <div class="mb-3">
            <label for="username" class="form-label" novalidate class="needs-validation">Username</label>
            <input 
            name="username"
            id="username"
            placeholder="enter username"
            type="text"
            class="form-control"
            required
            />
        </div>
        <div class="mb-3">
            <label for="email" class="form-label" novalidate class="needs-validation">Enter Your email</label>
            <input 
            name="email"
            id="email"
            placeholder="Add a catchy title"
            type="email"
            class="form-control"
            required
            />
        </div>
           <div class="mb-3">
            <label for="password" class="form-label" novalidate class="needs-validation">Password</label>
            <input 
            name="password"
            id="password"
            placeholder="enter your password"
            type="password"
            class="form-control"
            required
            />
        </div>
        <button type="submit" class="btn btn-dark">Signup</button>
      </form>
      </div>
      </div>